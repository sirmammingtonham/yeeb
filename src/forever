#!/usr/bin/python3
from subprocess import Popen

while True:
    print("\nStarting ")
    p = Popen("python3.8 -m pipenv run python3.8 bot.py", shell=True)
    return_code = p.wait()
    print(return_code)
    if return_code == 0:
        break